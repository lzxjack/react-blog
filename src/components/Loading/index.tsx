import { LoadingOutlined } from '@ant-design/icons';
import React from 'react';

import s from './index.scss';

const Loading: React.FC = () => <LoadingOutlined className={s.color} />;

export default Loading;
